server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GopalTyagi/Spring-Cloud-Config.git
          clone-on-start: true
            #Author Gopal Tyagi
